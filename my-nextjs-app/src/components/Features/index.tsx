import SectionTitle from "../Common/SectionTitle";
import SingleFeature from "./SingleFeature";
import featuresData from "./featuresData";

const Features = () => {
  return (
    <>
      <section id="features" className="py-16 md:py-20 lg:py-28">
        <div className="container">
          <SectionTitle
            title="Principales caractéristiques"
            paragraph="Découvrez des fonctionnalités avancées et une expérience utilisateur optimisée. Notre solution offre des outils puissants pour vous aider à atteindre vos objectifs plus rapidement et efficacement, tout en garantissant une utilisation simple et intuitive."
            center
          />

          <div className="grid grid-cols-1 gap-x-8 gap-y-14 md:grid-cols-2 lg:grid-cols-3">
            {featuresData.map((feature) => (
              <SingleFeature key={feature.id} feature={feature} />
            ))}
          </div>
        </div>
      </section>
    </>
  );
};

export default Features;
